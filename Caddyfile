{
	admin off
	auto_https off
	persist_config off
}

:{$PORT:8080} {
	root * /srv
	encode gzip
	file_server {
		hide /Caddyfile /Dockerfile /.dockerignore
	}
	header /.well-known/assetlinks.json Content-Type application/json
}
